
<% user.posts.each do |post| %>
     <blockquote>
        <%= content_tag :h4, post.title %>
        <%= content_tag :p, post.body %>
        <% if logged_in? %> <%= content_tag :footer, user.name %> <% end %>
      </blockquote>
<% end %>